### sempre sulle matrici

visto che so che la formula per calcolare l'elemento di una matrice è:
```
i = r*cols + c;
```
se voglio ottenere la riga dell'elemento:
```
r = f(i; cols) = i / cols;
```
mentre per ottenere la colonna dell'elemento:
```
c = g(i; cds) = i % cols;
```

ho messo alcuni esempi su vscode